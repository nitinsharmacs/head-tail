# src/headLib.js

## firstNLines
  ```
  firstNLines(lines, count)

  lines: Array
  count: Number

  return Array
  ```
## nByesFrom

  ```
  nBytesFrom(content, bytes)
  content: String
  bytes: Number

  return String
  ```

## head

  ```
  head(content, count) 

  content: String
  count: Number

  return String
  ```
